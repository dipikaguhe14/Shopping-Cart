import { MomentTimeAgoPipe } from './moment-time-ago.pipe';

describe('MomentTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new MomentTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
